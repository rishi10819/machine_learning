Use knn to predict sex based on height from dslabs "heights" data.
Partition into a training and test set of equal size. 
Use k values of seq(1, 101, 3) and calculate F_1 scores.

What is the max value of F_1?

At what value of k does the max occur?
